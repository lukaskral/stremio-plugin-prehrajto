# Port used by the Stremio addon HTTP server.
PORT=52932

# Optional /test endpoint access. Set all three values to enable diagnostics.
# Use a long, random token dedicated to /test; never reuse the proxy API key.
TEST_ENDPOINT_BEARER_TOKEN=
PREHRAJTO_DEBUG_USERNAME=
PREHRAJTO_DEBUG_PASSWORD=
